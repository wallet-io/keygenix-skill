# Keygenix Keys — Deployment Record

> ⚠️ Template — replace placeholders with your own generated keys.
> Private keys must NEVER be stored here — use `.env` only.

## Generated: YYYY-MM-DD

### API Auth Key
- **Public Key:** `<YOUR_API_AUTH_PUBLIC_KEY_HEX>`
- **Register on:** keygenix.pro dashboard → API Keys

### AuthKey
- **Public Key:** `<YOUR_AUTH_PUBLIC_KEY_HEX>`
- **Use as:** `authPubKey` when creating/importing keys

## How to Generate

```bash
node client.js keygen   # run twice — once for each keypair
```
